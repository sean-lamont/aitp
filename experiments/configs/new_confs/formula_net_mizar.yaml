defaults:
  - premise_selection

data_config:
  data_options:
    db: 'mizar40'

epochs: 30

exp_config:
    name: gcn_2

model_config:
  model_type: 'gcn'
  model_attributes:
    vocab_size: 13420
    embedding_dim: 128
    gnn_layers: 2
#    batch_norm: True

logging_config:
  project: mizar40
