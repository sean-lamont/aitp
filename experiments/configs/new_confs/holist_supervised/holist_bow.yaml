# @package _global_
defaults:
  - /experiments/holist_supervised@_here_

tac_embed_dim: 128
final_embed_dim: 1024
batch_size: 16

data_config:
  type: 'graph'

exp_config:
  name: bow

model_config:
  model_type: holist_gnn
  model_attributes:
    gnn_layers: 0
    vocab_size: 1500
    dropout: 0.2
    embedding_dim: 128