defaults:
  - premise_selection

data_config:
  data_options:
    db: 'hol4_original_ast'

exp_config:
  name: formula_net

model_config:
  model_type: formula-net-edges
  model_attributes:
    vocab_size: 2200
    embedding_dim: 256
    gnn_layers: 1
    batch_norm: False

logging_config:
  project: test_hydra
